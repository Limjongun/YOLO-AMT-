# ============================================================
# AMT-YOLO Requirements
# Hardware: RTX 4050 6GB VRAM
# Python:   3.12+
# CUDA:     12.x
# ============================================================

# --- Core Deep Learning ---
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0
# Install with CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# --- YOLO Base (YOLOv8) ---
ultralytics>=8.2.0

# --- Computer Vision ---
opencv-python>=4.10.0
Pillow>=10.3.0
numpy>=1.26.0

# --- Scientific Computing ---
scipy>=1.13.0
scikit-learn>=1.5.0
pandas>=2.2.0

# --- Tracking ---
# ByteTrack (install manually from source):
# git clone https://github.com/ifzhang/ByteTrack && pip install -e ByteTrack
# BoT-SORT included via ultralytics

# --- Experiment Tracking ---
wandb>=0.17.0
tensorboard>=2.17.0

# --- Configuration ---
PyYAML>=6.0.1
omegaconf>=2.3.0

# --- Visualization ---
matplotlib>=3.9.0
seaborn>=0.13.0
rich>=13.7.0                  # Beautiful terminal output

# --- Progress & Utilities ---
tqdm>=4.66.0
einops>=0.8.0                 # Tensor manipulation for attention modules
timm>=1.0.0                   # Pre-trained vision models

# --- Export & Deployment ---
onnx>=1.16.0
onnxruntime-gpu>=1.18.0
# TensorRT: install separately via NVIDIA TensorRT installer

# --- Notebook ---
jupyter>=1.0.0
ipywidgets>=8.1.0

# --- Testing ---
pytest>=8.2.0
pytest-cov>=5.0.0

# --- Code Quality ---
black>=24.4.0
isort>=5.13.0

# ============================================================
# Installation Notes for RTX 4050 6GB VRAM:
#
# 1. Install PyTorch with CUDA 12.1:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 2. Verify CUDA:
#    python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
#
# 3. Install this requirements:
#    pip install -r requirements.txt
#
# 4. Install package:
#    pip install -e .
# ============================================================
